import QtQuick

// Standalone dev entry: `qs -p .` from the repo root runs the service
// without the Omarchy shell. The plugin system itself only reads
// manifest.json and never touches this file.
Service {}
